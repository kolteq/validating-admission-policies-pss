apiVersion: v1
kind: Pod
metadata:
  name: readinessprobe-httpget-host-containers
  namespace: policy-test
spec:
  containers:
  - name: main
    image: busybox:1.36
    command:
    - sh
    - -c
    - sleep 3600
    readinessProbe:
      httpGet:
        path: /
        port: 80
        host: 127.0.0.1
