apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: check-startupprobe-httpget-host-initcontainers
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE","UPDATE"]
        resources: ["pods"]
  validations:
    - expression: >
        !has(object.spec.initContainers) ||
        object.spec.initContainers.all(ic,
          !has(ic.startupProbe) ||
          !has(ic.startupProbe.httpGet) ||
          !has(ic.startupProbe.httpGet.host) ||
          ic.startupProbe.httpGet.host == ""
        )
      message: >
        spec.initContainers[*].startupProbe.httpGet.host must be undefined or empty ("").
