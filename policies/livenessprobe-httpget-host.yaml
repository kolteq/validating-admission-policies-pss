apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: check-livenessprobe-httpget-host
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE","UPDATE"]
        resources: ["pods"]
  validations:
    - expression: >
        object.spec.containers.all(c,
          !has(c.livenessProbe) ||
          !has(c.livenessProbe.httpGet) ||
          !has(c.livenessProbe.httpGet.host) ||
          c.livenessProbe.httpGet.host == ""
        )
      message: >
        spec.containers[*].livenessProbe.httpGet.host must be undefined or empty ("").
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optout-warn
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Warn
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: validating-admission-policies.kolteq.com/warn
          operator: In
          values: ["baseline","restricted"]
        - key: validating-admission-policies.kolteq.com/livenessprobe-httpget-host
          operator: NotIn
          values: ["false"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optin-warn
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Warn
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: livenessprobe-httpget-host.validating-admission-policies.kolteq.com/warn
          operator: In
          values: ["true"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optout-audit
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Audit
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: validating-admission-policies.kolteq.com/audit
          operator: In
          values: ["baseline","restricted"]
        - key: validating-admission-policies.kolteq.com/livenessprobe-httpget-host
          operator: NotIn
          values: ["false"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optin-audit
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Audit
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: livenessprobe-httpget-host.validating-admission-policies.kolteq.com/audit
          operator: In
          values: ["true"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optout-deny
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Deny
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: validating-admission-policies.kolteq.com/enforce
          operator: In
          values: ["baseline","restricted"]
        - key: validating-admission-policies.kolteq.com/livenessprobe-httpget-host
          operator: NotIn
          values: ["false"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: check-livenessprobe-httpget-host-namespace-optin-deny
spec:
  policyName: check-livenessprobe-httpget-host
  validationActions:
    - Deny
  matchResources:
    namespaceSelector:
      matchExpressions:
        - key: livenessprobe-httpget-host.validating-admission-policies.kolteq.com/enforce
          operator: In
          values: ["true"]
