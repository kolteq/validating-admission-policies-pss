apiVersion: v1
kind: Pod
metadata:
  name: startupprobe-tcpsocket-host-initcontainers
  namespace: policy-test
spec:
  containers:
  - name: main
    image: busybox:1.36
    command:
    - sh
    - -c
    - sleep 3600
  initContainers:
  - name: init
    image: busybox:1.36
    command:
    - sh
    - -c
    - echo init
    restartPolicy: Always
    startupProbe:
      tcpSocket:
        port: 80
        host: 127.0.0.1
